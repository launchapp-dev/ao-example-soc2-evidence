# GitHub Personal Access Token
# Required for gh API calls in collection scripts and gh-cli-mcp
# Scopes needed: read:org, read:user, repo
# Get it at: https://github.com/settings/tokens
# Or run: gh auth login && export GH_TOKEN=$(gh auth token)
GH_TOKEN=your_github_token_here
